# Project setup

This is a dependency-free vanilla HTML/CSS/JavaScript website. The gallery is defined in `index.html`; there is no framework, build step, or package installation required.

## Run locally

Serve the project directory on port 5000:

```bash
python3 -m http.server 5000 --bind 0.0.0.0
```